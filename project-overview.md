# 數位車庫（Digital Garage）

### ——以車輛生命週期管理與愛車紀錄為核心之行動應用系統

---

## 一、專案背景與核心痛點

目前市面上的車輛管理 App 多以**單次油耗、加油記帳、基礎保養與維修流水帳**為主要功能，對一般通勤車主而言已具備一定實用性。然而，對於較重視愛車管理的使用者——例如改裝車主、汽機車愛好者、賽道與跑山玩家，以及希望長期完整保存車輛數據的車主而言，單純的記帳功能仍不足以描述一台車真實且立體的使用歷程。

在傳統用車與改裝管理中，車主經常面臨以下痛點：

* **數據割裂分散**：改裝零件規格存在通訊軟體對話紀錄中、保養工單收在實體手套箱抽屜、調校設定記在備忘錄或紙本上。
* **改裝歷程缺乏版本控制**：避震器段數、輪胎胎壓、傳動調校等設定隨季節或賽道調整，更動後往往遺忘前一版最佳設定值，無法進行參數差異對比（Diff Comparison）。
* **週期性規費與檢驗容易逾期**：牌照稅、公路養管費（燃料費）、定期檢驗窗口（出廠日前後各 1 個月）與保單到期日分散在不同月份，容易疏漏招致罰鍰。
* **里程數據缺乏權威防禦**：手動輸入容易誤打或逆向竄改，導致每公里平均成本與油耗指標計算失真。
* **長期持有成本（TCO）模糊**：購車費用、折舊、改裝投入、週期規費與定期消耗難以整合於單一時序牆呈現。

因此，本專案將車輛管理的概念從傳統的**「車輛流水記帳工具」**全面升級為**「個人數位車庫」**：

> **「一支手機，就是一座數位車庫。」**
> 使用者透過行動裝置，以單一車輛為核心實體，集中保存從新車／二手入庫、日常使用、保修維護、週期規費法定排程、改裝調校版本控制到長期持有成本分析的完整資料，讓車輛不只是代步工具，也是一份可持續累積與驗證的數位資產。

```text
Vehicle (單一車輛生命週期核心主體)
├── 基本規格 (型號、出廠日、車牌、排氣量、購車金額與初始里程基線)
├── 加油日誌 (加油量、單價、總額、燃油規格、純運算油耗指標)
├── 保修履歷 (保養/維修雙軌、工單實拍單據、店家資訊)
├── 保養提醒 (雙向里程/時間週期追蹤、三段狀態即時判定)
├── 週期規費 (牌照稅、公路養管費、定期檢驗窗口、強制險/任意險排程)
├── 改裝檔案 (改裝品規格、購買與安裝工資成本拆分、實裝照片)
│   └── 調校版本控制 (Snapshot 複製、單一生效版本保護、跨版本差異對比)
├── 動態時序牆 (動態整合 6 大事件之 4 級確定性排序時間軸)
└── 成本分析 (6/12/24 個月歷程、月份填零保護、TCO 與每公里運算)
```

---

## 二、專案目的與核心目標

本專案落地建置一套**高可用性、資料庫層級防禦、支援弱網離線作業與全端可觀測性**之現代化車輛管理系統，達成以下核心工程與產品目標：

1. **落實 Server-Authoritative 里程唯一權威**：
   以資料庫層級強制約束 `current_mileage >= initial_mileage`，由後端透過 `GREATEST` 原子更新聚合加油、保養與改裝安裝里程，杜絕里程竄改與除以零運算異常。
2. **建置全生命週期動態時序牆**：
   透過 PostgreSQL 視圖整合購車、加油、保養、維修、改裝與週期規費事件，落實 4 級確定性排序（日期、里程、建立時間、事件 ID）。
3. **提供改裝調校版本控制與差異比較**：
   支援設定版本深拷貝（Clone Snapshot）、Partial Unique Index 生效版本唯一性防護，以及前端 Map-based 參數差異對比演算法（Added, Removed, Changed, Unchanged）。
4. **支援台灣在地監理排程與智慧預填 (P2-2)**：
   完整支援 4 月牌照稅、7 月公路使用養護安全管理費（公路養管費）、車齡動態定期檢驗（前後 1 個月檢驗窗口推算），並以自建 `addMonthsClamped` 演算法根除閏年與大小月溢位 bug。
5. **提供弱網離線突變佇列防護 (Offline-First FIFO Queue)**：
   在地下停車場或山區訊號不佳時，支援加油、保養與提醒狀態本機安全快取（`expo-secure-store`），連線恢復自動循序同步，具備 4xx 永久錯誤隔離防塞車與 App 異常重啟復原機制。
6. **建立零信任端到端可觀測性鏈路 (P2-1)**：
   前後端由校驗之 `X-Request-ID` 串聯，後端全面採用 Google Cloud Logging 結構化日誌（`log/slog`）並對敏感金鑰進行深度脫敏，前端提供 Dark-Metal 全域錯誤邊界（`ErrorBoundary`）。

---

## 三、核心功能規格模組

目前系統已全數落地並通過驗收之功能模組架構如下：

| 功能模組 | 主要業務內容與工程特性 | 落地技術亮點 |
| :--- | :--- | :--- |
| **車輛管理** | 汽車／機車／其他車型支援；核心出廠日期、年份、排氣量、購車金額；車輛封面照片與自動遞補 | Dark-Metal 日期選擇器連動自動解析年份；入庫初始里程不可變保護 |
| **加油管理** | 加油量、單價、總額、燃油規格；百公里油耗（$\text{L}/100\text{km}$）與每公升公里數（$\text{km}/\text{L}$） | 純函式運算器；里程零增量除以零保護；樂觀推昇與伺服器覆蓋 |
| **保修履歷** | 雙軌分類（定期保養 vs 故障維修）；項目、施作店家、費用與里程；現場單據實拍多圖上傳 | 支援自動連動下次保養提醒；Modal 重繪防狀態覆寫保護（ISS-03） |
| **週期提醒** | 里程週期（`interval_km`）與月份週期（`interval_months`）雙軌追蹤；良好／即將到期／逾期判定 | 動態剩餘天數與里程推算；完成時基準點自動前移 |
| **週期規費** | 牌照稅、公路養管費、車齡定檢窗口、強制險與任意險；智慧預填當期/次期視窗 | 出廠日向後相容單一交易同步；`addMonthsClamped` 月末溢位截斷 |
| **改裝檔案** | 改裝品規格、購買價格與安裝工資成本拆分；安裝日期與里程；安裝實拍照上傳 | 費用分離歸屬月度成本；安裝里程參與權威里程計算 |
| **調校版本** | 單一零件多版本調校參數（如跑山、通勤、賽道）；生效版本唯一性；版本一鍵複製 Snapshot | Partial Unique Index；Map-based 跨版本參數差異比對演算法 |
| **動態時序牆** | 整合全生命週期 6 大事件；支援分類切換檢視；純淨購車入庫事件防偽造 | PostgreSQL View (`security_invoker = true`)；4 級確定性排序 |
| **成本統計** | 6 / 12 / 24 個月歷程圖表；總擁有成本（TCO）與總營運成本；每公里平均花費 | 月份自動填零保護；里程不足時安全回傳 `null` 防止除以零 |

---

## 四、現行技術架構與雲端拓撲

專案採用嚴謹的分層架構，落實前後端關注點分離與資料庫層級的多租戶安全防護：

```text
┌─────────────────────────────────────────────────────────────┐
│                 Mobile Client (Android Release)             │
│  - React Native 0.86.3 / Expo SDK 57 (TypeScript Strict)    │
│  - TanStack React Query 狀態管理與連線監控 (onlineManager)   │
│  - FIFO 永續離線突變佇列 + Expo SecureStore 分區快照        │
│  - 端到端 X-Request-ID 注入 + 30s 去重 ErrorReporter        │
│  - Dark-Metal 金屬科技 UI + ErrorBoundary 降級備援          │
└──────────────────────────────┬──────────────────────────────┘
                               │ HTTPS / Bearer Supabase JWT
                               ▼
┌─────────────────────────────────────────────────────────────┐
│                 Backend API (Google Cloud Run)              │
│  - Go 1.25.0 / Chi v5 高效能路由與中介層                    │
│  - Request Correlation ID 零信任輸入字元與長度校驗          │
│  - log/slog Google Cloud Logging JSON 結構化日誌            │
│  - 白名單機敏資料遞迴脫敏 (Exact Key Match Redaction)       │
│  - Server-Authoritative GREATEST 里程原子更新               │
│  - 單一 pgx.Tx 交易保證出廠日與規費一致性                   │
└──────────────────────────────┬──────────────────────────────┘
                               │ PostgreSQL 15 (Session Pooler)
                               ▼
┌─────────────────────────────────────────────────────────────┐
│                 Supabase Cloud Platform                     │
│  - PostgreSQL 15 (11 大業務資料表 + vehicle_timeline View)  │
│  - 1-Tier ~ 3-Tier 階層式 Row Level Security (RLS) 擁有權隔離│
│  - Supabase Auth (JWT 簽發與使用者會話管理)                 │
│  - Supabase Storage (車輛、工單、改裝實體照片儲存空間)      │
└─────────────────────────────────────────────────────────────┘
```

---

## 五、SWOT 產品與技術架構分析

| 分析構面 | 評估要點 |
| :--- | :--- |
| **Strengths（優勢）** | • **全生命週期整合**：超越傳統單純記帳，涵蓋改裝調校版本比對與規費法定時程。<br>• **工程防禦嚴密**：具備資料庫級 RLS、Server-Authoritative 里程保護與離線突變佇列。<br>• **在地化智慧合規**：深度貼合台灣監理時程（牌照稅、公路養管費、前後 1 個月定檢視窗）。<br>• **現代化雲原生架構**：Go 後端部署於 Cloud Run，搭配 Supabase 實現高效彈性伸縮。 |
| **Weaknesses（劣勢）** | • **資料建立門檻**：初期需要車主手動登錄初始資料，完整發揮效益需一段使用累積期。<br>• **相片離線暫存限制**：基於裝置儲存空間與上傳穩定度，圖片上傳目前僅限在線模式。 |
| **Opportunities（機會）** | • **車友社群與俱樂部**：車聚、賽道日或車友交流時可直接展示調校參數與改裝履歷。<br>• **二手車認證與轉移價值**：完整、不可竄改且附帶實拍單據的數位履歷大幅提升二手車交易信任度。<br>• **輕量化 AI 輔助整合**：未來可導入拍照 OCR 自動識別工單與行照、車況異音或油耗異常分析。 |
| **Threats（威脅）** | • **競品通用記帳功能同質化**：傳統記帳軟體可能追加車輛標籤，需持續凸顯改裝調校與時序履歷的專業護城河。<br>• **監理法規變更**：法規變更時須及時更新預填演算法。 |

---

## 六、產品商業價值與演進路徑

數位車庫的價值核心並非「單純記帳工具」，而是**「以車為本的高密度結構化履歷平台」**。

```text
車輛入庫 ──► 加油日誌 ──► 保養維修 ──► 週期規費 ──► 改裝調校 ──► 完整數位資產
```

### 潛在商業模式與擴充構想
1. **車況履歷報告匯出（Digital Vehicle Passport）**：
   車主在轉售愛車時，可一鍵匯出經過驗證的完整時序履歷與保養工單 PDF 報告，提高車輛殘值與買賣雙方透明度。
2. **店家合作與工單數位化**：
   合作保養廠或改裝店家可透過專屬介面或 QR Code 直送工單至車主的數位車庫，減少手動輸入負擔。
3. **改裝與消耗品採購推薦**：
   根據車輛型號、目前里程與已安裝零件壽命，提供合適的耗材週期更換提醒與原廠/改裝品配件資訊。
4. **輕量化 AI 智慧車庫助手**：
   * 行照與保養工單拍照 OCR 自動轉文字。
   * 基於歷史油耗曲線的異常偵測（如點火不良、積碳預警）。

---

## 七、專案現狀與驗收成果 (Project Baseline)

目前專案已完成從 P0 至 P2-2.5 之全階段開發與品質驗收，並具備生產交付標準：

* **靜態型別**：TypeScript 全專案 `strict: true`，`npm run typecheck` 0 錯誤。
* **單元測試**：Jest 前端測試共 26 套件、145 個測試案例全數通過；Go 後端測試 100% 通過。
* **品質審查**：通過生產級穩定性 15 大維度審查（含 RLS 多租戶隔離、月末溢位防護、離線狀態機閃退修復等）。
* **雲端部署**：Go 後端 API 穩定運行於 Google Cloud Run（`asia-east1`），並掛載健康檢查監控。
* **行動發布**：已成功編譯產出 Android 原生生產環境簽署 APK（`數位車庫_DigitalGarage.apk`）。

---

## 八、結語

> **「不是記錄你花了多少錢，而是保存你與這台車共同累積的每一段歷程。」**

數位車庫結合了高強度的軟體工程防禦、雲原生微服務架構與對汽機車文化的熱情，為每一位車主打造值得信賴的數位愛車夥伴。